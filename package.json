{
  "name": "anime-quiz",
  "version": "1.0.0",
  "description": "Système de quiz musical avec lobby et correction automatique",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon server.js",
    "client": "cd client && npm start",
    "build": "npm install && cd client && npm install && npm run build",
    "install-all": "npm install && cd client && npm install",
    "postinstall": "cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "mongoose": "^8.0.3",
    "multer": "^1.4.5-lts.1",
    "axios": "^1.6.2",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "concurrently": "^8.2.2"
  },
  "keywords": ["quiz", "music", "lobby", "railway"],
  "author": "AnimeQuiz",
  "license": "MIT"
}
